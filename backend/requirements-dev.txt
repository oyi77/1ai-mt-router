# Test-only dependencies (M35: aiosmtplib missing from test env broke conftest import)
# Install alongside runtime deps: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
aiosmtplib==5.1.2
flake8==7.3.0
